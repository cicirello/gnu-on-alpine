name: delete-old

on:
  workflow_dispatch:

jobs:

  delete:

    runs-on: ubuntu-latest
    
    steps:
    - name: Prune
      uses: vlaurin/action-ghcr-prune@v0.4.0
      with:
        token: ${{ secrets.DELETE_TOKEN }}
        organization: ""
        container: alpine-plus-plus
        dry-run: false # Dry-run first, then change to `false`
        older-than: 1 # days
        untagged: true
